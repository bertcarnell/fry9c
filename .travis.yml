language: r

matrix:
  include:
    - dist: bionic
      r: release
      apt_packages: libpoppler-cpp-dev
    - dist: xenial
      r: devel
      apt_packages: libpoppler-cpp-dev
    - os: osx
      r: release

cache: packages

sudo: false
