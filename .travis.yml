language: r

matrix:
  include:
    - dist: xenial
      r: release
      apt_packages: libpoppler-cpp-dev
    - dist: trusty
      r: devel
      apt_packages: libpoppler-cpp-dev
    - os: osx
      r: release

cache: packages

sudo: false
